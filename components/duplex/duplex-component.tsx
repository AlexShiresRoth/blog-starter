import { Duplex } from "@/types/duplex-component.type";
import React from "react";

type Props = {
  data: Duplex;
};

const DuplexComponent = ({ data }: Props) => {
  return <div>DuplexComponent</div>;
};

export default DuplexComponent;
